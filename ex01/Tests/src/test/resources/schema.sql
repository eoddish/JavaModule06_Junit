CREATE TABLE PRODUCT (
                               IDENTIFIER INT NOT NULL,
                               NAME VARCHAR(50) NOT NULL,
                               PRICE INT NOT NULL,
                               PRIMARY KEY (IDENTIFIER)
);